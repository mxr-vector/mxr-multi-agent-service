# 本地模型环境

- 本模块只负责加载本地模型提供给智能体服务层: multi-agent-base
  
参考：https://docs.vllm.com.cn/en/latest/getting_started/installation/gpu/#set-up-using-python

# 使用说明

```
uv pip install vllm --torch-backend=auto
```